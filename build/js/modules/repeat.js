const params = {
		
};

/**
 * @param { HTMLElement } multimedia Elemento contenedor de video.
 * @param { HTMLVideoElement } video.
*/
const repeat = (multimedia, video) => {
	if (!multimedia || !video) return;

	const __repeat = multimedia.dataset.repeat;
	if (!__repeat) return;


}

export default repeat;